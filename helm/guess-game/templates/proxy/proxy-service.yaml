apiVersion: v1
kind: Service
metadata:
  name: proxy-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: proxy
spec:
  selector:
    app: proxy
  ports:
  - port: 3001
    targetPort: 3001
    nodePort: 30300
  type: NodePort
